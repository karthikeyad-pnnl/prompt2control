# Documentation Feedback for Buildings.Templates.Plants.Controls.StagingRotation.StageChangeCommand

## Mandatory Edit Suggestions
These address direct violations of the documentation rules, such as description string format (must be short noun-phrase starting with upper-case, no trailing period) for parameters, variables, and connectors, and equation display (must use centered italic <p> for displayed equations).

| Original Text with Issue | Suggested Edit | Rationale |
|--------------------------|----------------|----------- |
| Parameter comments like: `True: The logic block is used in a hybrid heat pump plant` (e.g., for `have_HpShc`) | `Flag for hybrid heat pump plant logic` | Description strings must be short noun-phrases starting with upper-case, without sentences like "True:" or colons (rule 1); current is explanatory. Apply similarly to all Boolean parameters (e.g., `have_pumSec`: change to `Flag for primary-secondary distribution`; `have_inpPlrSta`: change to `Flag for input staging part load ratio signal`). |
| Parameter comments like: `Staging matrix for non-hybrid plant – Equipment required for each stage` (e.g., for `staEqu`) | `Staging matrix for non-hybrid plants` | Must be short noun-phrase without em-dashes or clauses (rule 1); current is fragmented. Apply to related params (e.g., `staEquSinMod`: change to `Staging matrix for hybrid single-mode plants`; `traStaEqu`: change to `Transpose of non-hybrid staging matrix`). |
| Connector comments like: `Stage available signal` (e.g., for `u1AvaSta`) | `Input for stage availability` | Connector descriptions must be noun-phrases; current is noun but tighten for consistency (rule 1). Apply to all interfaces (e.g., `uPlrSta`: change to `Input for staging part load ratio`; `y1Up`: change to `Stage up command output`; `TRet`: change to `Return temperature input for capacity calculation`). |
| Internal variable comments like: `Extract equipment required at given stage` (e.g., for `reqEquSta`) | `Extractor for equipment required at stage` | Internal descriptions must be noun-phrases, not verb phrases like "Extract" (rule 1); current violates by being action-oriented. Apply to all internals (e.g., `intScaRep`: change to `Integer signal replicator`; `capEquSta`: change to `Capacity of equipment at stage`; `gre`: change to `Greater comparator for OPLR vs. SPLR`; `hol`: change to `Holder for required capacity during staging`). |
| Displayed equation: `<p><i>OPLR = Qrequired / Qstage</i></p>` (not centered). | `<p align=\"center\" style=\"font-style:italic;\">OPLR = <i>Q<sub>required</sub></i> / <i>Q<sub>stage</sub></i></p>` | Rule 5 requires centered italic <p> for displayed equations with sub/superscripts; current is plain <p><i> without alignment or subscripts for clarity. Apply to similar inline math like <i>Qrequired</i> throughout (use <i>Q<sub>required</sub></i>). |
| Info lists: `<ul><li>Each stage has a minimum runtime of <code>dtRun</code>.(This condition is implemented in...</li>` (unwrapped text, >80 chars in some lines). | Wrap: `<ul><li><p>Each stage has a minimum runtime of <code>dtRun</code>.(This condition is implemented in <a href=\"modelica://Buildings.Templates.Plants.Controls.Utilities.StageIndex\">Buildings.Templates.Plants.Controls.Utilities.StageIndex</a>.)</p></li>...</ul>` | Text must be wrapped in <p> (rule 3); long lines exceed ~80 chars soft limit, and hyperlinks need modelica:// (rule 8, already good but ensure wrapping). Apply to all <ul> items in conditions and details. |
| Revisions dates: `<li>May 31, 2024, by Antoine Gautier:<br/>...</li>` (no <i> for date). | `<li><i>May 31, 2024</i>, by Antoine Gautier:<br/>...</li>` | Rule 9 requires <i> for dates in reverse chronological <ul><li>; current lacks italics. Apply to both entries. |

## Suggested Edits
These address potential missing information, such as optional info sections (e.g., Assumptions and limitations) or expansions on key parameters and hybrid logic.

| Original Text with Issue | Suggested Edit | Rationale |
|--------------------------|----------------|----------- |
| Info lacks "Assumptions and limitations" section; mentions timers reset but not explicit (e.g., "Timers are reset to zero at the completion of every stage change."). | Add after staging requirements list: `<h4>Assumptions and limitations</h4><p>Assumes primary sensors for capacity if both available; no hot-swapping during runtime. Hybrid mode requires separate matrices <code>staEquSinMod</code> and <code>staEquDouMod</code>.</p>` | Template suggests optional section (rule 4); clarifying sensor priority and hybrid assumptions improves understanding of logic limitations, especially for primary-secondary setups. |
| Short intro: `<p>The plant equipment is staged in part based on required capacity, <i>Qrequired</i>, relative to nominal capacity of a given stage, <i>Qstage</i>. This ratio is the operative part load ratio, <i>OPLR</i>.</p>` (defines terms but no block purpose). | Expand: `<p>Block generating stage up/down commands based on operative part load ratio (OPLR = <i>Q<sub>required</sub></i> / <i>Q<sub>stage</sub></i>), capacity, and availability.</p>` | Intro required (rule 4); adding block purpose ties to outputs <code>y1Up</code>/<code>y1Dow</code>, enhancing clarity; use subscripts (rule 5). |
| No "Typical use and important parameters" section; params like <code>dtRun</code>, <code>plrSta</code> mentioned inline but not grouped. | Add before "Details": `<h4>Typical use and important parameters</h4><p>Set <code>plrSta</code> (e.g., 0.8) for staging threshold; use <code>dtRun</code> (e.g., 5 min) for runtime. For hybrid plants, configure <code>have_HpShc=true</code> and provide mode-specific matrices.</p>` | Template optional section (rule 4); highlighting params like <code>dtMea</code>, <code>dT</code> guides tuning, especially for hybrid/primary-only via <code>have_pumSec</code>. |
| Hyperlink text: `<a href=\"modelica://Buildings.Templates.Plants.Controls.HeatPumps.AirToWater\">Buildings.Templates.Plants.Controls.HeatPumps.AirToWater</a>` (full path as text). | `<a href=\"modelica://Buildings.Templates.Plants.Controls.HeatPumps.AirToWater\">AirToWater controller</a>` | Rule 8 example uses descriptive text; full path is redundant—shorten for readability while keeping modelica:// URI. Apply to other links (e.g., FailsafeCondition: change to "FailsafeCondition block"). |
| Details: `<h4>Details</h4><p>A staging matrix <code>staEqu</code> is required as a parameter...` (good, but no equation for OPLR hold). | Add: `<p>During staging, <i>Q<sub>required</sub></i> held via <code>hol</code> block until <code>dtRun</code> or completion.</p>` | Enhances completeness; references internal <code>hol</code> (from variables) and ties to <code>dtRun</code> (rule 5 for inline <i>). |

## General Comments and Recommendations
- **Clarity and Structure**: Info flows well from OPLR definition to conditions, but dense lists could use a table for up/down triggers (rule 6: `<table summary="Staging conditions"><tr><th>Command</th><th>Conditions</th></tr><tr><td>Up</td><td>Availability, Efficiency (> <code>plrSta</code> for <code>dtRun</code>), Failsafe</td></tr>...</table>`). Define acronyms (OPLR, SPLR) on first use. Hyperlinks are consistent but add one to LoadAverage for capacity calc.
- **Conciseness**: Most lines <80 chars, but some <p> with hyperlinks wrap awkwardly—break with <br/>. Trim repetitions (e.g., "Runtime with exceeded staging part load ratio" in params to match info). Parameter grouping: Add `Dialog(group="Staging")` for matrices, `Dialog(group="Timing")` for <code>dtRun</code>, etc. (rule 2).
- **Compliance Gaps**: No figures (rule 7), but suggest staging flowchart: `<p align=\"center\"><img alt=\"Staging logic flowchart\" src=\"modelica://Buildings/Resources/Images/Templates/Plants/StagingRotation/StageChange.png\"/></p>` under Typical use. Equations use <i> correctly (rule 5), but more subscripts (e.g., <sub>req</sub>) for Q. Spell-check: Minor (e.g., "tranverse" in connections context? But not in info). For complex package, add "See Buildings.UsersGuide for plant staging" (rule 10).
- **Improvements**: Emphasize hybrid handling more (e.g., <code>u1HeaCoo</code> input). Ensure protected vars like transposed matrices have descriptions (current table good). Test for partial classes (no Dynamics). Overall, comprehensive but visuals and sections would aid complex users.