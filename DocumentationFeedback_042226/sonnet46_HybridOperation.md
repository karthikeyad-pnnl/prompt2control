# Documentation Review: Hybrid Plant Operation Controller Block

---

## Section 1: Mandatory Edits (Rules Being Broken)

| Original Text with Issue | Suggested Edit | Rationale |
|---|---|---|
| Revision entry `July 29, 2025, by Karthik Devaprasad:<br/>` is missing `<i>` tags around the date | Change to `<li><i>July 29, 2025</i>, by Karthik Devaprasad:<br/>` | Rule 9 explicitly requires the date in each revision list entry to be wrapped in `<i>…</i>` tags |
| The introductory paragraph text `"Block that manages custom calculations for integrating heat pump with simultaneous heating-cooling(SHC HP) with multiple modular single-mode heat pumps to create a hybrid heat pump plant."` is followed immediately by `<p>\n<ul>` — the `<ul>` is opened inside a `<p>` tag: `<p>\n<ul>\n<li>` | Close the introductory `<p>` before opening the `<ul>`: change `<p>\n<ul>` to `</p>\n<ul>` | Rule 3 requires text to be wrapped in `<p>…</p>`; a `<ul>` must not be a child of `<p>` as this is invalid HTML and will render incorrectly in some tools |
| The `<h4>Details</h4>` heading is not a permitted heading label from the Rule 4 template | Replace with `<h4>Implementation</h4>` | Rule 4 defines a fixed set of permitted `<h4>` headings; "Details" is not among them; the content describes internal logic choices and is correctly categorised under "Implementation" |
| The closing `</p>` tag after the final `</ul>` inside the `<h4>Details</h4>` section is misplaced: `</ul>\n</p>` — a `</p>` closes a `<p>` that was opened before the `<ul>`, which is invalid HTML | Remove the stray `</p>` after `</ul>`, since the opening `<p>` before the `<ul>` should not have been opened: change `<p>\nThe implemented module manages the following functions.\n<ul>` to `<p>The implemented module manages the following functions.\n</p>\n<ul>` | Rule 3 requires valid `<p>…</p>` wrapping; `<ul>` must not be nested inside `<p>` |
| The internal variable `intScaRep1` has description string `"Vectorize mode signal with dimnension equal to number of heat pumps"` — "dimnension" is a misspelling | Change to `"Vectorize mode signal with dimension equal to number of heat pumps"` | Rule 10 requires a spell check before committing; "dimnension" is a misspelling of "dimension" |
| The text references internal block names directly in prose using `<code>` tags (e.g., `<code>and2</code>`, `<code>swi</code>`, `<code>latHeaAva</code>`, `<code>and9</code>`, `<code>conInt1</code>`, `<code>conInt2</code>`) without any explanation of what these names mean to readers; furthermore `<code>conInt1</code>` and `<code>conInt2</code>` do not correspond to any named variable in the variable table (the actual names are `<code>conIntDir</code>` and `<code>conIntRev</code>`) | Replace `<code>conInt1</code>` with `<code>conIntDir</code>` and `<code>conInt2</code>` with `<code>conIntRev</code>` to match actual variable names; and replace internal block name references with functional descriptions | Rule 10 (spell/accuracy check): internal signal names referenced in documentation must match the actual Modelica identifiers; using wrong names creates misleading documentation |
| The parameter `have_sorRunTim` has description string `"Are the lead-lag equipment rotated based on runtime?"` — this is a question rather than a noun-phrase | Change to `"Set to true if lead/lag equipment rotation is based on runtime"` | Rule 1 requires description strings to start with an upper-case letter and be a short noun-phrase, not a question |

---

## Section 2: Suggested Edits (Possibly Missing Information)

| Original Text with Issue | Suggested Edit | Rationale |
|---|---|---|
| The introductory paragraph contains a grammatical error: `"integrating heat pump with simultaneous heating-cooling(SHC HP)"` — missing space before parenthesis, missing article, and awkward phrasing | Rephrase to: `"This block manages calculations required to integrate a simultaneous heating-cooling heat pump (SHC HP) with multiple single-mode heat pumps in a hybrid heat pump plant."` | The current sentence is grammatically incorrect and ambiguous; the suggested revision is clearer and follows the noun-phrase style of the Buildings library introductions |
| The first bullet under the introductory list contains a typographical error: `"shall nelead-lag controlled"` — missing space and word | Change to `"shall be lead-lag controlled"` | Rule 10 requires a spell/accuracy check; "nelead-lag" is clearly a typographical error for "be lead-lag" |
| No `<h4>Typical use and important parameters</h4>` section is present, yet `staEquDouMod`, `staEquSinMod`, `have_sorRunTim`, and `is_HpShc` are critical configuration parameters whose correct setting determines plant behaviour | Add `<h4>Typical use and important parameters</h4>` describing: `<code>staEquDouMod</code>` (staging matrix for simultaneous heating-cooling mode), `<code>staEquSinMod</code>` (staging matrix for single-mode operation), `<code>is_HpShc</code>` (Boolean vector identifying which HPs are SHC units), and `<code>have_sorRunTim</code>` (controls whether runtime-based lead/lag rotation is active) | Rule 4 recommends this section for important parameters; the interaction between these four parameters is non-trivial and undocumented |
| No `<h4>Assumptions and limitations</h4>` section is present; the block assumes exactly one SHC HP per plant, that the SHC HP always occupies the highest-index slot, and that single-mode HPs are 2-pipe units | Add `<h4>Assumptions and limitations</h4>` with at minimum: (1) only one SHC HP per plant is supported; (2) the SHC HP is assumed to occupy the last index in the HP vector; (3) the block does not handle faulted equipment | Rule 4 provides this section for non-obvious constraints; the single-SHC-HP assumption is critical for correct use and currently undocumented |
| The input signals `u1PumPriHea` and `u1PumPriCoo` have identical description strings: `"Primary pump enable for SHC HP"` — these are distinct signals for HW and CHW primary pumps respectively | Change `u1PumPriHea` description to `"Primary HW pump enable signal for SHC HP"` and `u1PumPriCoo` description to `"Primary CHW pump enable signal for SHC HP"` | Rule 1 requires description strings to be unambiguous; identical descriptions for distinct signals that represent different fluid loops is misleading |
| The output `y1PumPri` has the description `"Primary pump enable for SHC HP"` — same ambiguity as the inputs above; it is also identical to both input descriptions | Change to `"Primary pump enable command for SHC HP in heating-cooling mode"` | Rule 1 requires description strings to be distinct and accurate; the output activates only in heating-cooling mode (per `and9`) and this distinction should be captured |

---

## Section 3: General Comments and Recommendations

| Original Text with Issue | Suggested Edit | Rationale |
|---|---|---|
| The `defaultComponentName` is `"ctlPlaHyb"` which is correct lowerCamelCase | No change needed | Rule 10 compliance confirmed |
| The `<h4>Implementation</h4>` section (currently `<h4>Details</h4>`) describes internal block names and their interconnections at a very low level of detail, making it difficult to read without simultaneously consulting the diagram | Consider restructuring the Implementation section into three short sub-paragraphs matching the three bullet points in the introduction: (1) mode determination and staging matrix selection; (2) availability latch logic; (3) primary pump enable logic | Aligning the Implementation narrative structure with the introductory bullet list makes the documentation easier to follow and reduces the need to cross-reference the diagram |
| The abbreviation "SHC HP" is introduced in the title paragraph but is not expanded on first use in a way that is unambiguous — "simultaneous heating-cooling(SHC HP)" lacks a space and uses informal abbreviation style | Change to `"simultaneous heating-cooling heat pump (SHC HP)"` on first use, then use `SHC HP` consistently throughout | Consistent, properly expanded abbreviations on first use follow standard technical writing practice and improve readability for new users |
| The phrase `"if <code>has_sort=false</code>"` in the Implementation section references a parameter `has_sort` that does not appear in the variable table — the actual parameter is `<code>have_sorRunTim</code>` | Change `<code>has_sort=false</code>` to `<code>have_sorRunTim=false</code>` | Rule 10 accuracy check: the parameter name must match the actual Modelica identifier; using a non-existent name creates a documentation error |
| The description string for parameter `is_HpShc` reads `"Vector indicating if each HP is an SHC HP; True=Is SHC HP;False=Not SHC HP"` — the semicolon-separated true/false explanation is non-standard for a description string | Change to `"Vector of flags indicating which heat pumps are SHC units (true) and which are single-mode units (false)"` | Rule 1 requires short noun-phrases; the current string is too long and uses non-standard punctuation; the revised version is concise and informative |
| No `<h4>Validation</h4>` section exists; for a complex hybrid plant controller, referencing test or example models would greatly help users | If validation models exist, add `<h4>Validation</h4>` with `modelica://` links to the relevant test cases | Rule 4 lists `<h4>Validation</h4>` as optional but strongly beneficial for complex blocks |