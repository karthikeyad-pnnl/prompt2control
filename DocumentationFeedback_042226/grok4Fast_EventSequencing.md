# Documentation Feedback for Buildings.Templates.Plants.Controls.StagingRotation.EventSequencing

## Mandatory Edit Suggestions
These address direct violations of the documentation rules, such as description string format (must be short noun-phrase starting with upper-case, no trailing period) and HTML wrapping (text in lists should be in <p> for consistency).

| Original Text with Issue | Suggested Edit | Rationale |
|--------------------------|----------------|----------- |
| Parameter comments like: `Set to true for plants that provide HW` (e.g., for `have_heaWat`) | `Flag for plants providing hot water` | Description strings must be short noun-phrases starting with upper-case, without sentences like "Set to true" or trailing periods (rule 1); current is explanatory sentence. Apply similarly to all Boolean parameters (e.g., `have_chiWat`: change to `Flag for plants providing chilled water`; `have_HpShc`: change to `Flag for hybrid heat pump plants`). |
| Connector comments like: `Enable command from heating mode sequence` (e.g., for `u1Hea`) | `Heating mode enable command input` | Connector descriptions must follow noun-phrase rule; current is phrase but could be tightened for brevity (rule 1). Apply to all interfaces (e.g., `u1PumHeaWatPri_actual`: change to `Primary hot water pump status input`; `y1ValHeaWatInlIso`: change to `Hot water inlet isolation valve command output`). |
| Internal variable comments like: `Return true when nominal valve timing elapsed` (e.g., for `timVal`) | `Timer output for elapsed nominal valve timing` | Internal variable descriptions must be noun-phrases, not sentences starting with "Return true" (rule 1); current violates by being imperative/explanatory. Apply to all internals (e.g., `heaValPum`: change to `Logical AND for heating valves and pumps`; `ena`: change to `Enable signal for equipment`). |
| Info HTML lists: `<ul><li>The isolation valves for desired heating or cooling mode are commanded open.</li>...</ul>` (text not wrapped in <p>) | Wrap list items in <p>: `<ul><li><p>The isolation valves for desired heating or cooling mode are commanded open.</p></li>...</ul>` | All text should be wrapped in `<p>...</p>` for consistency (rule 3); unwrapped list items may render poorly and violate soft line limits (~80 chars) when combined with <br/>. Apply to both heating and off-state lists. |
| Hyperlink text: `<a href=\"modelica://Buildings.Templates.Plants.Controls.Pumps.Generic.StagingHeadered\">Buildings.Templates.Plants.Controls.Pumps.Generic.StagingHeadered</a>` (full path as link text) | `<a href=\"modelica://Buildings.Templates.Plants.Controls.Pumps.Generic.StagingHeadered\">StagingHeadered</a>` | Hyperlinks should use descriptive text, not full path (implied by rule 8 example); repeating full name is redundant and reduces clarity. Apply to both instances. |

## Suggested Edits
These address potential missing information, such as optional info sections (e.g., Assumptions) or references to important parameters, to improve completeness.

| Original Text with Issue | Suggested Edit | Rationale |
|--------------------------|----------------|----------- |
| Info lacks "Assumptions and limitations" section; implies assumptions like valve timing but not explicit (e.g., "Once the isolation valves are fully open (based on nominal valve timing <code>dtVal</code>)"). | Add after off-state list: `<h4>Assumptions and limitations</h4><p>Assumes isolation valves fully open in <code>dtVal</code> time and pumps prove on immediately. No fault handling for stuck valves or pumps.</p>` | Info template suggests optional "Assumptions and limitations" section (rule 4); documenting timing and proofing assumptions clarifies model behavior, especially for sequencing logic. |
| Info short intro: `<p>If a heat pump is commanded on in a desired heating or cooling mode:</p>` (starts directly with conditional, no overall purpose). | Prepend: `<p>Block sequencing events for heat pump enable/disable, including valves and pumps.</p><p>If a heat pump is commanded on in a desired heating or cooling mode:</p>` | Short intro required (rule 4); current jumps into details—adding a one-sentence overview enhances clarity without length, tying to sequencing purpose. |
| Revisions: Single entry `<li>March 29, 2024, by Antoine Gautier:<br/>First implementation.</li>` (minimal detail). | Expand: `<li><i>March 29, 2024</i>, by Antoine Gautier:<br/>First implementation, including valve/pump sequencing and latches.</li>` | Revisions should describe changes (rule 9); adding brief scope aids traceability for this initial version, especially for complex sequencing. |
| No mention of key parameters like `dtOff` in info beyond inline `<code>dtOff</code>`. | Add under "Options" if inserted: `<h4>Options</h4><p>Configure timing with <code>dtVal</code> (valve open time) and <code>dtOff</code> (shutdown delay, default 3 min).</p>` | Template suggests optional "Options" or "Typical use and important parameters" (rule 4); referencing params like `dtVal` and `dtOff` (used in logic) would guide users on customization. |
| Info uses `<b>Plants with dedicated primary pumps</b>` (bold for emphasis). | Change to `<p><i>Plants with dedicated primary pumps</i></p>` (or keep but ensure italics for terms). Inline: `<i>Plants with dedicated primary pumps</i>` | Rule 5 suggests `<i>` for inline variables/terms; `<b>` is non-standard here—switch to italics for consistency with equation/inline styles. |

## General Comments and Recommendations
- **Clarity and Structure**: The info is logically structured with on/off states, but lists could be more scannable—consider numbering steps or adding a simple table for valve/pump actions (rule 6: include `summary` like "Sequencing steps"). Hyperlinks to StagingHeadered are appropriate but ensure path exists. Overall, clear for technical users, but define acronyms (e.g., HW/CHW) on first use for broader accessibility.
- **Conciseness**: HTML is mostly under 80 chars per line, but combined <li> content could wrap better. Trim repetitive phrases like "commanded open/closed" by using a table. Internal comments are fixed in mandatory, but variables table could group by type (e.g., via parameter grouping in code, rule 2: add `Dialog(group="Timing")` for `dtVal`, `dtOff`).
- **Compliance Gaps**: No equations or figures (rules 5/7), but a flow diagram for sequencing (e.g., `<p align=\"center\"><img alt=\"Event sequencing flowchart\" src=\"modelica://Buildings/Resources/Images/Templates/Plants/StagingRotation/EventSeq.png\"/></p>`) would enhance if available. Spell-check OK (no errors). For complex package, add reference: "See Buildings.UsersGuide for plant control overview" (rule 10).
- **Improvements**: Test for partial classes (no Dynamics needed). Ensure all protected vars have descriptions (seem complete). Prioritize adding optional sections to match template fully, improving navigation. This block's documentation is solid but would benefit from visual aids for sequencing logic.