# Documentation Review: Equipment Enable Command Block

---

## Section 1: Mandatory Edits (Rules Being Broken)

| Original Text with Issue | Suggested Edit | Rationale |
|---|---|---|
| Revision entry `March 29, 2024, by Antoine Gautier:<br/>` has no `<i>` tag wrapping the date | Change to `<li><i>March 29, 2024</i>, by Antoine Gautier:<br/>` | Rule 9 explicitly requires the date in each revision list entry to be wrapped in `<i>…</i>` tags |
| Inline numeric literals used with `<i>` tags throughout the bullet list: `equal to <i>0</i>`, `equal to <i>1</i>`, `lower than <i>1</i>`, `greater than <i>0</i>` | Replace all with plain text: `equal to 0`, `equal to 1`, `strictly lower than 1`, `strictly greater than 0` | Rule 5 reserves `<i>…</i>` exclusively for inline variable names; numeric literal values must not be italicised |
| The mathematical sum expression `∑_j staEqu[i, j]` and the condition `∑_j staEqu[i+1, j] &ge; ∑_j staEqu[i, j]` and `i &lt; size(staEqu, 1)` appear as plain Unicode/HTML entities mixed into prose text inside `<li>` elements, with no consistent markup for the variable names | Wrap all variable and index references in `<i>` or `<code>` as appropriate, and render the summation condition as a display equation: `<p align=\"center\" style=\"font-style:italic;\">∑<sub>j</sub> staEqu[i+1, j] ≥ ∑<sub>j</sub> staEqu[i, j]</p>` with individual variable references such as `<i>i</i>` and `<code>staEqu</code>` marked up inline | Rule 5 requires display equations to use the centred italic `<p>` pattern and inline variable names to use `<i>`; parameter names such as `staEqu` should use `<code>` |
| The description string for internal parameter `conTraMatStaEquDouMod` reads `"Constant signal for tranverse of staging equation matrix in heating-cooling mode"` — "tranverse" is a misspelling | Change to `"Constant signal for transpose of staging equation matrix in heating-cooling mode"` | Rule 10 requires a spell check; "tranverse" should be "transpose" |
| The description string for internal parameter `conTraMatStaEquSinMod` reads `"Constant signal for tranverse of staging equation matrix in single-operation mode"` — same misspelling | Change to `"Constant signal for transpose of staging equation matrix in single-operation mode"` | Rule 10 requires a spell check; same error as above |

---

## Section 2: Suggested Edits (Possibly Missing Information)

| Original Text with Issue | Suggested Edit | Rationale |
|---|---|---|
| No `<h4>Typical use and important parameters</h4>` section is present, yet three distinct staging matrices (`staEqu`, `staEquSinMod`, `staEquDouMod`) are required parameters whose selection depends on plant type | Add `<h4>Typical use and important parameters</h4>` explaining that `<code>staEqu</code>` is used for non-hybrid plants, `<code>staEquSinMod</code>` and `<code>staEquDouMod</code>` are used for hybrid plants in single-operation and heating-cooling modes respectively, and that the input `<code>u1HeaCoo</code>` controls the active matrix | Rule 4 recommends this section when important parameters require user guidance; the three-matrix structure is non-obvious and critical for correct configuration |
| No `<h4>Assumptions and limitations</h4>` section is present; the block explicitly assumes that stage indices are monotonically non-decreasing in equipment count (`∑_j staEqu[i+1,j] ≥ ∑_j staEqu[i,j]`) | Add `<h4>Assumptions and limitations</h4>` stating: (1) the staging matrix must satisfy the monotonicity constraint on equipment count across stages; (2) the block does not handle faulted equipment — availability is managed entirely via `<code>u1Ava</code>`; (3) hot-swapping of equivalent lead/lag alternate equipment is suppressed to avoid unnecessary cycling | Rule 4 provides this section for non-obvious constraints; the monotonicity requirement is already mentioned in the `<ul>` list but belongs in a dedicated section for visibility |
| No `<h4>Implementation</h4>` section is present; the mechanism for suppressing enable signal updates between stage changes (the latch-and-switch logic using `logSwi`, `swiEna`, `y1Pre`) is a non-trivial implementation choice | Add `<h4>Implementation</h4>` explaining that enable signals are held at their previous values between stage changes using a `Pre` block and a logical switch (`<code>logSwi</code>`), and that updates are triggered only on stage change, on a mode change (`<code>u1HeaCoo</code>`), or when the number of available previously-enabled units falls below the required count | Rule 4 provides `<h4>Implementation</h4>` for this purpose; the suppression logic is the most complex aspect of the block and should be explained explicitly |
| The introductory paragraph mentions `uIdxAltSor` as "indices of lead/lag alternate equipment, sorted by increasing staging runtime" but does not explain how this input interacts with the staging matrix coefficients between 0 and 1 | Expand the introductory paragraph or add a sentence in `<h4>Typical use and important parameters</h4>` clarifying that `<code>uIdxAltSor</code>` provides the priority order for selecting which lead/lag alternate equipment (those with `0 < staEqu[i,j] < 1`) to enable when multiple candidates are available | The current text leaves users unclear on how runtime sorting interacts with the matrix fractional coefficients, which is essential for understanding lead/lag rotation behaviour |
| The parameter `is_pumApp` ("Is the equipment enable block used for a pump control module application?") is not mentioned anywhere in the `info` section despite being a configuration parameter | Add a note in `<h4>Typical use and important parameters</h4>` briefly describing the effect of `<code>is_pumApp</code>` on block behaviour | Rule 1 requires all parameters to be adequately described; since this Boolean affects internal logic, its effect should be documented in the `info` section |

---

## Section 3: General Comments and Recommendations

| Original Text with Issue | Suggested Edit | Rationale |
|---|---|---|
| The `defaultComponentName` is set to `"enaEqu"` which is correct lowerCamelCase | No change needed | Rule 10 compliance confirmed |
| The bullet list describing the staging matrix coefficient semantics is detailed and well-structured; however the final two bullets (the summation rule and the monotonicity condition) are more mathematical than the preceding bullets and would read more clearly as a separate sub-section or display equation block | Move the summation and monotonicity conditions into a `<h4>Main equations</h4>` section using proper display equation markup, keeping the bullet list for the qualitative coefficient semantics only | Rule 4 provides `<h4>Main equations</h4>` for exactly this purpose; separating qualitative rules from quantitative constraints improves readability |
| The phrase "hot swapping equipment" is used informally without definition: "This avoids hot swapping equipment, e.g., an equipment would not be started and another stopped during operation just to fulfill the priority order" | Rephrase to: "This avoids unnecessary equipment cycling, i.e., stopping one unit and starting another solely to satisfy the lead/lag priority order during steady-state operation" | "Hot swapping" is an informal term that may not be recognised by all HVAC engineers; "unnecessary equipment cycling" is more precise in this context. Also, "an equipment" is grammatically incorrect — "a unit" or "a piece of equipment" should be used |
| The phrase "strictly less than the number of equipment required to run" uses "equipment" as a count noun without a quantifier, which is grammatically ambiguous | Change to "strictly less than the number of units required to run" | Consistent use of "units" rather than "equipment" as a count noun improves grammatical clarity throughout the documentation |
| The description string for `traStaEqu` reads `"Transpose of staging matrix for non-hybrid plant"` — this internal parameter's description is acceptable but could include the relationship to `staEqu` | Optionally change to `"Transpose of staging matrix staEqu for non-hybrid plant"` | Minor improvement to make the relationship between `traStaEqu` and `staEqu` explicit in the description string, aiding maintenance |
| No `<h4>Validation</h4>` section exists; for a complex staging logic block, referencing any test models would help users and reviewers | If test or example models exist, add `<h4>Validation</h4>` with `modelica://` links | Rule 4 lists `<h4>Validation</h4>` as optional but recommended for complex logic blocks |