# Documentation Feedback for Buildings.Templates.Plants.Controls.StagingRotation.EquipmentEnable

## Mandatory Edit Suggestions
These address direct violations of the documentation rules, such as description string format (must be short noun-phrase starting with upper-case, no trailing period) for all parameters, variables, and connectors.

| Original Text with Issue | Suggested Edit | Rationale |
|--------------------------|----------------|----------- |
| Parameter comments like: `Is the equipment enable block used for a pump control module application?` (e.g., for `is_pumApp`) | `Flag for pump control module application` | Description strings must be short noun-phrases starting with upper-case, without questions or sentences (rule 1); current is interrogative. Apply similarly to all Boolean parameters (e.g., `have_HpShc`: change to `Flag for hybrid heat pump plant logic`; `nEquAlt`: change to `Number of lead/lag alternate equipment`). |
| Parameter comments like: `Staging matrix for non-hybrid plant – Equipment required for each stage` (e.g., for `staEqu`) | `Staging matrix for non-hybrid plants` | Must be short noun-phrase without em-dashes or explanatory clauses (rule 1); current is fragmented sentence. Apply to related params (e.g., `staEquSinMod`: change to `Staging matrix for hybrid single-mode operation`; `traStaEqu`: change to `Transpose of non-hybrid staging matrix`). |
| Connector comments like: `Detect plant switching to heating-cooling mode` (e.g., for `u1HeaCoo`) | `Input for detecting heating-cooling mode switch` | Connector descriptions must follow noun-phrase rule; current is imperative verb phrase (rule 1). Apply to all interfaces (e.g., `u1Ava`: already good as "Equipment available signal", but tighten `uIdxAltSor`: change to `Sorted indices of lead/lag alternate equipment`; `y1`: change to `Equipment enable command output`). |
| Internal variable comments like: `Extract equipment required at given stage` (e.g., for `reqEquSta`) | `Extractor for equipment required at stage` | Internal descriptions must be noun-phrases, not sentences starting with verbs like "Extract" (rule 1); current violates by being action-oriented. Apply to all internals (e.g., `intScaRep`: change to `Integer signal replicator`; `nEquStaRea`: change to `Sum of equipment required at stage`; `ena`: change to `Logical OR for equipment enable conditions`). |
| Info list items: `<ul><li>Each row of this matrix corresponds to a given stage.</li>...` (unwrapped text in <li>) | Wrap in <p>: `<ul><li><p>Each row of this matrix corresponds to a given stage.</p></li>...</ul>` | All text should be wrapped in `<p>...</p>` (rule 3); unwrapped <li> content may not render consistently and violates wrapping rule. Apply to all <ul> items, including coefficient explanations and conditions. |

## Suggested Edits
These address potential missing information, such as optional info sections (e.g., Typical use) or references to parameters, to enhance completeness.

| Original Text with Issue | Suggested Edit | Rationale |
|--------------------------|----------------|----------- |
| Info lacks "Assumptions and limitations" section; implies matrix constraints like `<code>∑_j staEqu[i+1, j] &ge; ∑_j staEqu[i, j]</code>` but not structured. | Add after list: `<h4>Assumptions and limitations</h4><p>Assumes monotonically non-decreasing equipment per stage. No support for hot-swapping during operation unless availability changes.</p>` | Template recommends optional "Assumptions and limitations" (rule 4); extracting inline math conditions clarifies constraints, aiding users in matrix setup. Use `<code>` for sums (rule 5). |
| Short intro: `<p>This block generates the equipment enable commands based on the active stage index <code>uSta</code>, the equipment available signal <code>u1Ava</code> and the indices of lead/lag alternate equipment, sorted by increasing staging runtime.</p>` (long sentence). | Shorten: `<p>Block generating equipment enable commands from stage index <code>uSta</code>, availability <code>u1Ava</code>, and sorted alternate indices.</p>` | Intro required but should be concise (rule 4); current exceeds ~80 chars in one line (rule 3)—splitting improves readability while retaining key inputs. |
| No "Typical use and important parameters" section; matrices described but params like `nEquAlt` not highlighted. | Add: `<h4>Typical use and important parameters</h4><p>Use <code>staEqu</code> for non-hybrid; set <code>nEquAlt</code> for alternates. See staging matrices for row/column definitions.</p>` | Template suggests optional section (rule 4); referencing params like `nSta`, `nEqu` guides configuration, especially for hybrid vs. non-hybrid use. |
| Info math: `<i>0</i>`, `<i>1</i>` in lists (inline italics for numbers). | Use `<code>0</code>`, `<code>1</code>` for coefficients: `<li>A coefficient <code>staEqu[i, j]</code> equal to <code>0</code> means...</li>` | Rule 5 specifies `<i>` for variables, but `<code>` for parameter/code references like matrix values; improves consistency with `<code>staEqu</code> usage. Apply to all coefficients (0, 1, >0 <1). |
| Revisions: `<li>March 29, 2024, by Antoine Gautier:<br/>First implementation.</li>` (lacks italics for date). | `<li><i>March 29, 2024</i>, by Antoine Gautier:<br/>First implementation for stage-based equipment enabling.</li>` | Rule 9 requires `<i>` for dates; adding brief description enhances traceability without verbosity. |

## General Comments and Recommendations
- **Clarity and Structure**: Info is detailed on matrices but dense—consider a table for coefficient meanings (rule 6: `<table summary="Staging matrix coefficients"><tr><th>Coefficient</th><th>Meaning</th></tr><tr><td><code>0</code></td><td>Not required</td></tr>...</table>`) to break up text. Use subscripts consistently (e.g., `<sub>i</sub>`, `<sub>j</sub>` for indices, rule 5). Flow is logical, but define "lead/lag alternate" earlier for non-experts.
- **Conciseness**: HTML lines mostly <80 chars, but list items could be shortened (e.g., combine sum conditions). Avoid repetition of "lead/lag alternate" by hyperlinking once to a guide if available (rule 8). Parameter grouping in code could use `Dialog(group="Staging matrices")` for `staEqu*` (rule 2).
- **Compliance Gaps**: No figures (rule 7), but a matrix example image (e.g., `modelica://Buildings/Resources/Images/Templates/Plants/StagingRotation/MatrixExample.png` with `alt="Sample staging matrix"`) would clarify. No equations section (rule 5), but add displayed sum: `<p align="center" style="font-style:italic;">∑<sub>j</sub> staEqu[i, j] ≥ ∑<sub>j</sub> staEqu[i+1, j]</p>` under Assumptions. Spell-check OK; no FIXMEs.
- **Improvements**: For complex logic, reference User's Guide (rule 10: "See Buildings.UsersGuide for staging overview"). Ensure all protected vars (e.g., transposed matrices) have descriptions—current table covers them. Test rendering for math in lists. Overall, strong technical depth, but visuals and sections would boost usability.