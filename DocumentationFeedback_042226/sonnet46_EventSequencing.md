# Documentation Review: Staging Event Sequencing Block

---

## Section 1: Mandatory Edits (Rules Being Broken)

| Original Text with Issue | Suggested Edit | Rationale |
|---|---|---|
| The revision entry `March 29, 2024, by Antoine Gautier:<br/>` has no `<i>` tag wrapping the date | Change to `<li><i>March 29, 2024</i>, by Antoine Gautier:<br/>` | Rule 9 explicitly requires the date in each revision list entry to be wrapped in `<i>…</i>` tags |
| The `info` section contains no short introductory paragraph describing what the block is; the documentation begins immediately with a conditional behaviour description ("If a heat pump is commanded on...") | Add a short introductory `<p>` before the first `<ul>`, e.g., `<p>This block implements the staging event sequencing for heat pump enable and disable transitions, including isolation valve and primary pump commands.</p>` | Rule 4 states that a short introductory paragraph is **required** in every `info` section |
| The parameter `dtOff` description string in the variable table reads `"Heat pump internal shutdown cycle timing"`, but in the `info` HTML the same parameter is described with a different name: `"time required for the internal shutdown cycle to time out"` and the code reference used is `<code>dtOff</code>`. However, the introductory text also refers to `<code>dtVal</code>` saying it is the "nominal valve timing" — neither `dtVal` nor `dtOff` are wrapped in `<code>` tags consistently: `(<code>dtOff</code> to be determined empirically, defaulting to <i>3</i>&nbsp;min)` — the value `3` is wrapped in `<i>` but should not be, as `<i>` is reserved for variable names per Rule 5 | Change `<i>3</i>` to plain text `3`, keeping the `&nbsp;min` unit: `(<code>dtOff</code> to be determined empirically, defaulting to 3&nbsp;min)` | Rule 5 reserves `<i>…</i>` for inline variable names only; numeric literal values must not be italicised |
| The parameter `have_valInlIso` description string reads `"Set to true if the system as inlet isolation valves"` — grammatical error ("as" should be "has") | Change to `"Set to true if the system has inlet isolation valves"` | Rule 1 requires description strings to be correct; this is a grammatical error making the string misleading |
| The parameter `have_valOutIso` description string reads `"Set to true if the system as outlet isolation valves"` — same grammatical error | Change to `"Set to true if the system has outlet isolation valves"` | Rule 1 requires description strings to be correct; same grammatical error as above |

---

## Section 2: Suggested Edits (Possibly Missing Information)

| Original Text with Issue | Suggested Edit | Rationale |
|---|---|---|
| No `<h4>Typical use and important parameters</h4>` section is present, yet parameters `dtVal`, `dtOff`, and `dtFil` are critical for correct sequencing behaviour | Add a `<h4>Typical use and important parameters</h4>` section describing `<code>dtVal</code>` (nominal valve timing), `<code>dtOff</code>` (shutdown cycle timing, default 3 min, to be set empirically), and `<code>dtFil</code>` (filter delay for upstream enable signals) | Rule 4 recommends this section for important parameters; all three timing parameters directly control sequencing thresholds and should be documented here rather than only in inline list text |
| The parameter `dtFil` ("Time delay for filtering out random enable signals from upstream") is mentioned nowhere in the `info` HTML, even though it affects the enable path via `truDelHea` and `truDelCoo` blocks | Add a description of `<code>dtFil</code>` in the sequencing description or in a `<h4>Typical use and important parameters</h4>` section, e.g., "Enable commands from the upstream sequence are filtered with a true delay of duration `<code>dtFil</code>` to suppress spurious pulses." | The omission leaves users unaware of this filtering step, which can affect commissioning behaviour; all parameters that influence sequencing logic should be documented |
| No `<h4>Assumptions and limitations</h4>` section is present; the block assumes that valve travel time is fixed at `dtVal` and that shutdown cycle duration must be determined empirically, both of which are non-obvious constraints | Add `<h4>Assumptions and limitations</h4>` with at minimum: (1) valve travel time is assumed constant and equal to `<code>dtVal</code>`; (2) `<code>dtOff</code>` must be set empirically for the specific heat pump model | Rule 4 lists this as an optional but useful section; the empirical nature of `dtOff` is already noted in the list text and warrants a dedicated section for visibility |
| No `<h4>Implementation</h4>` section is present; the use of latch blocks (`latValHeaWatIso`, `latValChiWatIso`, `latPumHeaWatPri`, `latPumChiWatPri`) to keep valves and pumps active during shutdown is a non-trivial implementation detail | Add `<h4>Implementation</h4>` briefly explaining that latch blocks hold valve-open and pump-run commands until the shutdown timer `<code>dtOff</code>` expires, and that `PlaceholderLogical` blocks are used to handle optional signals not present in all plant configurations | Rule 4 provides `<h4>Implementation</h4>` for exactly this purpose; describing the latch mechanism helps future maintainers understand why direct signal routing is not used |
| The `info` text refers only to "dedicated primary pumps" and "headered primary pumps" but does not mention secondary pumps (`have_pumHeaWatSec`, `have_pumChiWatSec`), which are present as inputs (`u1PumHeaWatSec_actual`, `u1PumChiWatSec_actual`) and are used in the `heaValPum` and `cooValPum` MultiAnd conditions | Add a note clarifying that when secondary pumps are present, their status (`u1PumHeaWatSec_actual`, `u1PumChiWatSec_actual`) is also required to be proven on before the heat pump is enabled | The omission creates a discrepancy between what the documentation describes and what the logic actually requires, which could lead to incorrect commissioning expectations |

---

## Section 3: General Comments and Recommendations

| Original Text with Issue | Suggested Edit | Rationale |
|---|---|---|
| The `defaultComponentName` is set to `"seqEve"` which is correct lowerCamelCase | No change needed | Rule 10 compliance confirmed |
| The two hyperlinks to `Buildings.Templates.Plants.Controls.Pumps.Generic.StagingHeadered` are identical and appear in both the enable and disable bullet lists; the link text and `modelica://` URI are correctly formatted | No change needed for link formatting; consider consolidating the repeated cross-reference into a single mention in an `<h4>Implementation</h4>` section to reduce duplication | Rule 8 compliance is confirmed; reducing repetition improves conciseness |
| The `<b>Plants with dedicated primary pumps</b>` and `<b>Plants with headered primary pumps</b>` labels are used as bold inline headings inside `<li>` elements; this is a reasonable pattern but could be replaced with `<h5>` sub-headings if the section grows | No mandatory change; if the section is expanded in future, consider restructuring with `<h5>` sub-headings for each plant type | Rule 3 allows `<h5>` sparingly; the current bold approach is acceptable for short lists but `<h5>` would improve navigability for longer descriptions |
| The phrase "to be determined empirically, defaulting to `3` min" appears only in the `<ul>` list text and not in the parameter description string for `dtOff` | Update the description string for `dtOff` from `"Heat pump internal shutdown cycle timing"` to `"Heat pump internal shutdown cycle timing (to be set empirically)"` | Rule 1 requires description strings to be informative short noun-phrases; adding the empirical qualification directly in the description string ensures it appears in tool tooltips without requiring users to read the full `info` section |
| The overall `info` section has no opening introductory sentence, making it harder for users browsing the library to understand the block's purpose at a glance without reading the full bullet lists | Add a one-sentence introductory `<p>` as noted in Section 1; this also satisfies Rule 4 | A clear introductory sentence is both a mandatory rule requirement and good documentation practice for complex sequencing blocks |
| The revisions section contains only a single entry; the structure is otherwise correct (reverse-chronological `<ul><li>` format) once the `<i>` date wrapper is added | No further change beyond the mandatory `<i>` wrapper fix noted in Section 1 | Rule 9 compliance will be satisfied after applying the mandatory fix |