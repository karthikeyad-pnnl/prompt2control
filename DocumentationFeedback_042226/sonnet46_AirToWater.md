# Documentation Review: Air-to-Water Heat Pump Plant Controller

---

## Section 1: Mandatory Edits (Rules Being Broken)

| Original Text with Issue | Suggested Edit | Rationale |
|---|---|---|
| `<table summary=\"summary\" border=\"1\">` — `cellspacing`, `cellpadding`, and `style` attributes are missing from the table tag | `<table summary=\"Supported plant configurations\" border=\"1\" cellspacing=\"0\" cellpadding=\"2\" style=\"border-collapse:collapse;\">` | Rule 6 requires all four attributes: `border`, `cellspacing`, `cellpadding`, and `style=\"border-collapse:collapse;\"` on every table element |
| `<h4>Details</h4>` — heading label does not match any permitted heading from the template | Replace with `<h4>Implementation</h4>` | Rule 4 defines a fixed ordered list of permitted `<h4>` headings. "Details" is not among them; "Implementation" is the correct heading for describing internal logic and naming conventions |
| The `<br/>` tag appears after the introductory paragraph: `...ASHRAE, 2021 for chiller plants.\n</p>\n<p>\nThe supported plant configurations are enumerated in the table below.<br/>\n</p>` — trailing `<br/>` inside a `<p>` block before a table | Remove the `<br/>` so the paragraph reads: `<p>\nThe supported plant configurations are enumerated in the table below.\n</p>` | Rule 3 requires text wrapped in `<p>…</p>`; a trailing `<br/>` immediately before `</p>` is redundant and violates clean HTML practice. The paragraph tag already provides the needed separation |
| Revisions entries use date format `January 23, 2025` but the rule example shows `January 15, 2024` with day before comma — the current entries are correctly formatted; however the `<i>` tag wraps only the date and not the full introductory phrase. Confirm: `<li>\nJanuary 23, 2025, by Antoine Gautier:<br/>` — the `<i>` wrapper is missing entirely around the date | Wrap each date in `<i>`: `<li><i>January 23, 2025</i>, by Antoine Gautier:<br/>` | Rule 9 explicitly requires the date to be wrapped in `<i>…</i>` tags in each revision list entry |
| The `summary` attribute value is a generic placeholder: `summary=\"summary\"` | Change to a descriptive value, e.g., `summary=\"Supported plant configurations for air-to-water heat pump plants\"` | Rule 6 requires a meaningful `summary` attribute on every table to support accessibility |

---

## Section 2: Suggested Edits (Possibly Missing Information)

| Original Text with Issue | Suggested Edit | Rationale |
|---|---|---|
| The `<h4>Details</h4>` section covers naming conventions and current limitations but mixes two conceptually distinct topics in a single section | Split into two sections: `<h4>Implementation</h4>` for the naming convention note about `pumHeaWatPri*`, and `<h4>Assumptions and limitations</h4>` for the statement about no fault-handling logic | Rule 4 lists both `<h4>Assumptions and limitations</h4>` and `<h4>Implementation</h4>` as standard optional sections. Separating them improves navigability and matches the prescribed template order |
| No `<h4>Typical use and important parameters</h4>` section is present, yet the model has a large number of configuration parameters (e.g., `staEqu`, `nHp`, distribution type, pump arrangement) that are critical for correct use | Add a `<h4>Typical use and important parameters</h4>` section describing the key parameters a user must set, particularly `staEqu`, plant function, distribution type, and pump arrangement options | Rule 4 lists this as an optional but recommended section; given the complexity of the configuration space, its absence leaves users without essential setup guidance |
| The introductory paragraph refers to "ASHRAE, 2021" inline without a hyperlink or cross-reference to the `<h4>References</h4>` entry using the `id` anchor | Change the inline citation to `[<a href=\"#ASHRAE2021\">ASHRAE, 2021</a>]` to link to the existing `<li id=\"ASHRAE2021\">` anchor | Consistent cross-referencing between inline citations and the References section improves traceability and follows standard documentation practice used elsewhere in the Buildings library |
| The `<h4>References</h4>` list item uses `<li id=\"ASHRAE2021\">` but the closing tag is `</li>` with a blank line before it — the full title string is very long and may exceed the soft 80-character line limit | Break the reference text across multiple lines at natural phrase boundaries, keeping each line ≤ 80 characters | Rule 3 specifies a soft limit of ~80 characters per line for readability in source editors |
| No mention of the `staEqu` staging matrix format or how to construct it appears in the `info` section itself; only a cross-reference link is provided | Add one or two sentences in the `<h4>Typical use and important parameters</h4>` section summarising the matrix convention (rows = stages, columns = equipment) before directing users to the linked class | The cross-reference to `EquipmentEnable` is good practice but a brief summary in the parent class avoids forcing users to navigate away for basic understanding |
| The limitation statement ("no logic for handling faulted equipment") is placed under `<h4>Details</h4>` (to be renamed `<h4>Implementation</h4>`) | Move this statement to a dedicated `<h4>Assumptions and limitations</h4>` section | Rule 4 provides `<h4>Assumptions and limitations</h4>` specifically for this kind of constraint; placing it under Implementation conflates two distinct concerns |
| The note on variable primary plant defrost behaviour ("Most AWHPs on the market use a reverse cycle for defrosting…") is embedded inside a table cell | Consider moving this explanatory text to the `<h4>Typical use and important parameters</h4>` or `<h4>Implementation</h4>` section as a paragraph, with only a short summary retained in the table cell | Long prose inside `<td>` cells is hard to read and difficult to maintain; the guideline text exceeds the soft 80-character line limit many times over inside the table |

---

## Section 3: General Comments and Recommendations

| Original Text with Issue | Suggested Edit | Rationale |
|---|---|---|
| The `defaultComponentName` annotation is set to `"ctl"` which is correct lowerCamelCase; however, no verification is possible from the documentation alone that this name is used consistently in example models | Confirm that any example or validation model instantiating this controller uses `ctl` as the default instance name | Rule 10 requires `defaultComponentName` in lowerCamelCase for drag-and-drop classes; consistency with instantiated examples avoids confusion |
| Table notes cells contain very long paragraphs with multiple `<br/>` line breaks used as paragraph separators within a single `<td>` | Replace multi-paragraph `<td>` content that uses `<br/>` as a paragraph break with proper `<p>…</p>` tags inside the `<td>` | Rule 3 requires text wrapped in `<p>…</p>`; using `<br/>` as a substitute paragraph separator is non-compliant and reduces accessibility |
| The word "appplications" appears in the variable description for `dTHea`: `"Delta-T triggering stage up command for heating appplications (>0)"` | Correct to `"Delta-T triggering stage up command for heating applications (>0)"` | Rule 10 requires a spell check before committing; this is a clear typographical error in a description string |
| The overall `info` section is well-structured and the revision history is in correct reverse-chronological order, which is commendable | No change needed; maintain this structure in future revisions | Rule 9 compliance is confirmed for ordering; only the missing `<i>` date wrappers need correction (addressed in Section 1) |
| The hyperlink to `EquipmentEnable` uses the correct `modelica://` URI scheme and displays the fully qualified name as link text, which is correct | No change needed | Rule 8 compliance is confirmed for this link |
| Several parameter description strings use em-dash (`–`) characters directly (e.g., `"Design heat pump heating capacity - Each heat pump"`), while others use a plain hyphen | Standardise separator style across all parameter descriptions; the plain hyphen (`-`) is more portable in Modelica string literals; alternatively use a consistent em-dash entity if HTML rendering is intended | Consistency in description string formatting improves readability in tool tooltips and generated documentation |
| No `<h4>Validation</h4>` section exists; for a controller of this complexity it is helpful to reference any validation or test models | If validation examples exist under `Buildings.Templates.Plants`, add `<h4>Validation</h4>` with links using `modelica://` URIs | Rule 4 lists `<h4>Validation</h4>` as an optional section; for a complex plant controller, referencing test cases aids users and reviewers significantly |