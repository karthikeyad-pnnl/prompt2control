# Documentation Feedback for Buildings.Templates.Plants.Controls.AirToWaterHeatPumps.Controller

## Mandatory Edit Suggestions
These address direct violations of the documentation rules, such as description string format (must be short noun-phrase starting with upper-case, no trailing period), HTML table attributes, and hyperlink formats.

| Original Text with Issue | Suggested Edit | Rationale |
|--------------------------|----------------|----------- |
| Variable comments like: `Set to true if the staging order algorithm includes the sequence of sorting runtime for equipment rotation` (e.g., for `have_sorRunTim`) | `Flag to include sorting by equipment runtime in staging order` | Description strings must be short noun-phrases starting with upper-case, without sentences like "Set to true" or trailing periods; current is explanatory sentence, violating rule 1. Apply similarly to all Boolean parameters (e.g., `have_heaWat`: change to `Flag for plants providing hot water`). |
| Variable comments like: `Heating mode enable via schedule` (e.g., for `u1SchHea`) | `Heating enable schedule input` | Connector descriptions must follow noun-phrase rule; "via schedule" is unnecessary explanation, making it less concise (rule 1). Apply to all interface comments (e.g., `u1PumHeaWatPri_actual`: change to `Primary hot water pump status input`). |
| Table in info: `<table summary=\"summary\" border=\"1\">` (missing full attributes) | `<table summary=\"Supported plant configurations\" border=\"1\" cellspacing=\"0\" cellpadding=\"2\" style=\"border-collapse:collapse;\">` | Tables must include `summary` (descriptive, not placeholder), `cellspacing=\"0\"`, `cellpadding=\"2\"`, and `style=\"border-collapse:collapse;\"` (rule 6); current lacks these, breaking HTML structure. |
| Revisions hyperlinks: `<a href=\"https://github.com/lbl-srg/modelica-buildings/issues/4432\">#4432</a>` | Remove or change to plain text `#4432` (or if keeping, use external link format without `modelica://` but note it's non-standard) | Hyperlinks must use `modelica://` for fully qualified Modelica names (rule 8); GitHub URLs are external and violate this—simplify to text for compliance, as external links aren't specified. Apply to both `#4432` and `#3808`. |

## Suggested Edits
These address potential missing information, such as optional info sections (e.g., Assumptions) or clarifications for completeness, without strict violations.

| Original Text with Issue | Suggested Edit | Rationale |
|--------------------------|----------------|----------- |
| Info lacks dedicated "Assumptions and limitations" section; last `<p>` mentions: `At its current stage of development, this controller contains no logic for handling faulted equipment. It is therefore assumed that any equipment is available at all times.` | Add after "Details": `<h4>Assumptions and limitations</h4><p>This controller assumes all equipment is available, with no fault handling. See variables table for configuration flags.</p>` | Info template recommends optional sections like "Assumptions and limitations" in order (rule 4); moving this content enhances structure and highlights limitations, improving usability. |
| Info short intro: `<p>This block implements the sequence of operation for plants with air-to-water heat pumps. Most parts of the sequence of operation are similar to that described in ASHRAE, 2021 for chiller plants.</p>` | Expand slightly: `<p>Block implementing sequence of operations for air-to-water heat pump plants, similar to ASHRAE Guideline 36-2021 for chiller plants.</p>` | Short intro is present but could reference the table earlier for better flow; adds conciseness while tying to supported configurations (rule 4 requires short intro; this clarifies without adding length). |
| Table "Notes" column has long entries, e.g., for "Type of distribution": multi-line explanation exceeding ~80 chars per line in some wrapped text. | Break into shorter `<p>`-like structure inside `<td>` or use `<br/>` more: `<td>Assumed same for HW and CHW loops.<br/>Most AWHPs use reverse cycle defrosting, requiring max primary flow.<br/>... (shorten sentences)</td>` | HTML lines should be ≤~80 chars (rule 3); long notes reduce readability—splitting improves conciseness and compliance. |
| Revisions lacks entry for initial implementation details beyond "First implementation." | Add to March 29, 2024 entry: `<li>March 29, 2024, by Antoine Gautier:<br/>First implementation for basic heating/cooling staging.</li>` | Revisions should be reverse chronological with descriptive changes (rule 9); current is minimal—adding brief scope aids traceability without verbosity. |

## General Comments and Recommendations
- **Clarity and Structure**: The info section is mostly clear, with a logical flow from intro to table to details. However, the table dominates and could benefit from a figure (e.g., plant schematic) under a new "Typical use" section to visualize configurations (rule 7: use `modelica://` PNGs with `alt` text). Add `<p align=\"center\"><img alt=\"Plant schematic\" src=\"modelica://Buildings/Resources/Images/Templates/Plants/AWHP/Config.png\"/></p>` if image exists.
- **Conciseness**: Descriptions in variables table are often verbose (e.g., full sentences); standardize to 5-10 words max for all. Info HTML is concise but could trim repetitive phrases like "This option is only available for..." in table notes by using bullet-like `<ul>` inside `<td>`.
- **Compliance Gaps**: No displayed equations (rule 5), but none needed—consider adding if key logic (e.g., staging PLR) is formalized. Ensure spell-check (rule 10): minor issues like "chiller plants" (consistent) but "AWHPs" assumes acronym knowledge—define on first use. For complex package, add User's Guide reference (rule 10).
- **Improvements**: Group related variables in info (e.g., mention staging parameters under "Typical use"). Test HTML rendering for line wraps. Overall, documentation is strong on references but could expand optional sections for better completeness.