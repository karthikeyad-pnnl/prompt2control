# Documentation Review: `seqEve` — Heat Pump Sequencing Events

---

## Section 1: Mandatory Edits (Rules Being Broken)

| Original Text / Issue | Suggested Edit | Rationale |
|---|---|---|
| `<b>Plants with dedicated primary pumps</b>` (used twice) | `<b>Plants with dedicated primary pumps</b>` → replace `<b>` with `<strong>` is acceptable but more importantly: no rule is broken here per se — however `<b>` is not lower-case compliant since `<b>` is lower-case. No change needed for case. **Actual issue**: bold is used for sub-headings inside a `<ul>` list; this is a structural misuse. Move these sub-categories to a `<p>` or restructure as a nested description. | Rule 3: HTML tags must be lower-case (tags are lower-case here, so compliant), but bold text used as a pseudo-heading inside a list item is poor semantic HTML. Sub-headings inside lists should use `<h5>` outside the list or plain text differentiation. |
| `<a href=\"modelica://Buildings.Templates.Plants.Controls.Pumps.Generic.StagingHeadered\">` appears twice with the same link text `Buildings.Templates.Plants.Controls.Pumps.Generic.StagingHeadered` | Verify the link is fully qualified and uses `modelica://` — it does. **However**, the same hyperlink block appears in two separate `<li>` items in different lists with no structural issue, but the `href` uses escaped quotes `\"` which is Modelica string escaping, not HTML. In the rendered HTML the quotes will be correct. No change needed. **Actual mandatory issue**: The `info` section text begins directly with a conditional statement (`If a heat pump is commanded on...`) without a **short introductory paragraph** describing what the block does overall. | Rule 4: The `info` section **requires** a short introduction paragraph before any other content. The current first `<p>` jumps directly into conditional logic rather than describing the component's purpose. |
| First `<p>` reads: `If a heat pump is commanded on in a desired heating or cooling mode:` | Add a new introductory paragraph before all existing content, e.g.: `<p>This block implements the enable/disable sequence for heat pumps in a plant, including commands for isolation valves and primary pumps in both heating and cooling modes.</p>` | Rule 4: A short introduction is **required** as the first element of the `info` section. |
| The `have_valInlIso` parameter comment reads: `Set to true if the system as inlet isolation valves` | Change to: `Set to true if the system has inlet isolation valves` | Rule 1 (description string quality) and general spell-check (Rule 10): "as" should be "has". |
| The `have_valOutIso` parameter comment reads: `Set to true if the system as outlet isolation valves` | Change to: `Set to true if the system has outlet isolation valves` | Same as above — "as" should be "has". Spell-check rule (Rule 10). |
| Variable description string `y1ValHeaWatInlIso` reads: `Inlet HW inlet isolation valve command` | Change to: `HW inlet isolation valve command` | Rule 1: Description strings must be short and accurate. "Inlet HW inlet" is a redundant duplication of "Inlet". |

---

## Section 2: Suggested Edits (Missing or Incomplete Information)

| Original Text / Issue | Suggested Edit | Rationale |
|---|---|---|
| No `<h4>Typical use and important parameters</h4>` section present | Add section: `<h4>Typical use and important parameters</h4><p>The parameters <code>dtVal</code> and <code>dtOff</code> govern the timing of valve actuation and heat pump shutdown respectively. The parameter <code>dtFil</code> sets a delay to filter spurious enable signals from upstream sequences.</p>` | Rule 4: This section is optional but strongly recommended when key parameters (`dtVal`, `dtOff`, `dtFil`) directly govern behaviour described in the `info` text. Currently `dtVal` and `dtOff` are referenced inline without a dedicated parameter description section. |
| No `<h4>Options</h4>` section present | Add section: `<h4>Options</h4><p>The Boolean parameters <code>have_pumHeaWatPri</code>, <code>have_pumChiWatPri</code>, <code>have_pumHeaWatSec</code>, <code>have_pumChiWatSec</code>, <code>have_valInlIso</code>, and <code>have_valOutIso</code> configure the sequence for plants with different pump and valve configurations.</p>` | Rule 4: The `Options` section should document the conditional Boolean configuration parameters that alter block behaviour. These parameters are present in the model but not described in the `info` text. |
| The parameter `dtFil` (time delay for filtering random enable signals) is not mentioned anywhere in the `info` documentation | Add a mention of `<code>dtFil</code>` in the new `Typical use and important parameters` section or inline in the main description: `<p>A true delay of <code>dtFil</code> is applied to the heating and cooling enable commands to filter out short upstream signal pulses before acting on them.</p>` | Rule 4 / Rule 1: Parameters should be explained in context. `dtFil` directly drives `truDelHea` and `truDelCoo` which feed the latch inputs, so it is functionally important but currently undocumented in the `info` section. |
| The `<h4>Implementation</h4>` section is absent | Add section: `<h4>Implementation</h4><p>Latch blocks are used to maintain valve and pump commands active until the heat pump internal shutdown timer (<code>dtOff</code>) has elapsed. The signal <code>u1HeaOrCoo</code> drives the nominal valve timing timer, and both heating and cooling paths are handled symmetrically.</p>` | Rule 4: An `Implementation` section is recommended when non-obvious internal logic (latches, timers, placeholder signals) is used. This aids maintainability. |
| No `<h4>Assumptions and limitations</h4>` section | Add section: `<h4>Assumptions and limitations</h4><p>This sequence assumes that valve end-of-travel is indicated solely by the nominal timing parameter <code>dtVal</code>, without a physical end-of-travel feedback signal.</p>` | Rule 4: Assumptions should be documented, particularly that valve open/close confirmation is time-based rather than feedback-based, which is a meaningful limitation. |
| `dtOff` description in text: `(<code>dtOff</code> to be determined empirically, defaulting to <i>3</i>&nbsp;min)` — the use of `<i>` for a numeric value `3` is inconsistent | Change to: `(<code>dtOff</code> to be determined empirically, defaulting to <i>3</i> min)` — or more consistently, remove `<i>` from the numeral and write: `(parameter <code>dtOff</code>, to be determined empirically; default: 3 min)` | Rule 5: `<i>` is for variable names/symbols. A plain numeral like `3` should not be wrapped in `<i>`. The `&nbsp;` non-breaking space before `min` is good practice and should be retained. |

---

## Section 3: General Comments and Recommendations

| Original Text / Issue | Suggested Edit | Rationale |
|---|---|---|
| The overall `info` section is written as two sequential conditional procedure lists (`If commanded on…` / `If commanded off…`) with no high-level summary | Prepend a 1–2 sentence summary paragraph describing the block's overall purpose (sequencing of heat pump enable/disable with valve and pump coordination), then retain the two lists. | Good documentation practice: readers scanning the `info` section need immediate context before procedural detail. This also satisfies the mandatory Rule 4 introduction requirement. |
| The `<b>…</b>` tags used for `Plants with dedicated primary pumps` and `Plants with headered primary pumps` sub-labels inside `<li>` items create visually reasonable but semantically flat structure | Consider restructuring these into nested `<ul>` lists or using `<h5>` sub-headings outside the lists, or at minimum keeping `<b>` but ensuring it is consistently applied across both the "on" and "off" sequences (it is — this is consistent). No mandatory change, but a nested list would be cleaner. | Readability and semantic HTML quality. Nested `<ul>` with indented sub-items for pump type would better convey the conditional branching structure. |
| The revision entry: `March 29, 2024, by Antoine Gautier` — date format is `Month DD, YYYY` | The date format is compliant with Rule 9. No change needed. Confirm spelling of author name before release (Rule 10). | Rule 9 compliance confirmed. Flagged for spell-check completeness per Rule 10. |
| Inline reference to `<code>dtVal</code>` in the text `based on nominal valve timing <code>dtVal</code>` is good | Retain as-is. This is a good example of Rule 5 compliance for parameter references. | Positive note — correct use of `<code>` for parameter names. |
| No `defaultComponentName` review needed — `defaultComponentName="seqEve"` is present and uses lowerCamelCase | Compliant. No change needed. | Rule 10 satisfied. |