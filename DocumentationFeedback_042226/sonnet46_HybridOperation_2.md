# Documentation Review: `ctlPlaHyb`

---

## Section 1: Mandatory Edits (Rules Being Broken)

| # | Original Text with Issue | Suggested Edit | Rationale |
|---|--------------------------|----------------|-----------|
| 1 | `<ul>` list appears directly after the first `<p>` block without being wrapped in its own `<p>` tag: `</p>\n<p>\n<ul>` — the `<ul>` is inside a `<p>` tag | Move the `<ul>` outside of the `<p>` tags: `</p>\n<ul>\n<li>…</li>\n</ul>` | Block-level elements such as `<ul>` must not be nested inside `<p>` elements. The closing `<p>` should appear before the `<ul>` opens, and no wrapping `<p>` should surround the list. |
| 2 | `<h4>Details</h4>` — a custom heading name is used instead of the prescribed heading vocabulary | Replace with one of the standard headings, e.g. `<h4>Implementation</h4>` | Rule 4 specifies the permitted heading sequence. `Details` is not in the list; `Implementation` is the correct heading for describing how the block is realised internally. |
| 3 | Second `<ul>` list (under `<h4>Details</h4>`) is also wrapped inside a `<p>` element: `<p>\nThe implemented module manages the following functions.\n<ul>` | Split into `<p>The implemented module manages the following functions.</p>\n<ul>\n<li>…</li>\n</ul>` | Same rule as item 1: `<ul>` is a block element and must not be a child of `<p>`. |
| 4 | The closing `</p>` after the second `</ul>` is a stray tag with no matching open: `</ul>\n</p>` | Remove the stray `</p>` | After extracting the `<ul>` from inside `<p>`, the orphan closing tag must be deleted to keep the HTML well-formed. |
| 5 | Abbreviation `SHC HP` is introduced in the opening paragraph but never expanded inline in the `info` section | At first use write: `simultaneous heating-cooling heat pump (SHC HP)` | Good documentation practice and general readability; readers unfamiliar with the acronym have no in-file reference. |
| 6 | `intScaRep1` variable comment contains a typo: `dimnension` | Change to `dimension` | Rule 10 (spell check) requires all spelling errors to be corrected before merging. |
| 7 | Parameter `staEquSinMod` description string contains no space before the em-dash: `cooling-only mode– Equipment` | Change to `cooling-only mode – Equipment required for each stage` | Consistent punctuation and readability; the missing space makes the description string malformed visually. |

---

## Section 2: Suggested Edits (Missing or Incomplete Information)

| # | Original Text with Issue | Suggested Edit | Rationale |
|---|--------------------------|----------------|-----------|
| 1 | Opening paragraph does not describe the inputs and outputs of the block | Add a brief sentence such as: `<p>It accepts heating-plant enable signal <code>u1EnaHea</code> and cooling-plant enable signal <code>u1EnaCoo</code> as primary inputs, and produces the staging matrix <code>yStaEqu</code>, mode vector <code>yMod</code>, availability vectors <code>yAvaHpShcHea</code> and <code>yAvaHpShcCoo</code>, and primary-pump enable <code>y1PumPri</code> as outputs.</p>` | Rule 4 requires a short but informative introduction. Listing key ports helps users understand the block interface at a glance. |
| 2 | No `<h4>Typical use and important parameters</h4>` section is present | Add a section explaining that `staEquDouMod` and `staEquSinMod` must be supplied by the user, and noting the effect of `have_sorRunTim` | Rule 4 lists this heading as optional but recommended. Parameters `staEquDouMod`, `staEquSinMod`, and `have_sorRunTim` directly govern block behaviour and warrant explicit user guidance. |
| 3 | No `<h4>Assumptions and limitations</h4>` section | Add a section noting that the block assumes exactly one SHC HP is present in the plant (`is_HpShc` vector) and that `nHp ≥ 1` | Rule 4 lists this as an optional but recommended section. The single-SHC-HP assumption is a non-obvious constraint that users need to know. |
| 4 | The description of `latHeaAva` and `latCooAva` in the Implementation paragraphs does not explain the clear condition | Extend the sentence to: `The latches are cleared on the falling edge of the HP enable signal <code>u1Hp</code> (detected by <code>falEdg</code>), i.e., when the SHC HP is disabled.` | The clear mechanism is implemented in the model (`connect(falEdg.y, latHeaAva.clr)` and `connect(u1Hp, falEdg.u)`) but is absent from the documentation, leaving the latch behaviour incompletely described. |
| 5 | The `revisions` entry uses `<i>July 29, 2025</i>` with no `<br/>` on the same line as the author credit | Change to: `<li>\n<i>July 29, 2025</i>, by Karthik Devaprasad:<br/>\nFirst implementation.\n</li>` | Rule 9 requires the `<br/>` separator between the date/author line and the description text. |
| 6 | Duplicate connector descriptions for `u1PumPriHea` and `u1PumPriCoo` — both read `Primary pump enable for SHC HP` | Change `u1PumPriCoo` description to `Primary pump enable for SHC HP cooling loop` and `u1PumPriHea` to `Primary pump enable for SHC HP heating loop` | Rule 1 requires every connector to have a meaningful, unique description string. Identical strings for distinct connectors are ambiguous. |
| 7 | No explanation of the integer encoding used for `yMod` (e.g., which integer value corresponds to heating-only, cooling-only, heating-cooling) | Add to the `Implementation` section: `<p>The operation mode <code>yMod</code> is encoded as an integer: heating-only = <i>1</i>, cooling-only = <i>2</i>, heating-cooling = <i>3</i>.</p>` (adjust values to match `conInt`, `conInt3`, `conInt5` constants) | The integer encoding is essential for users connecting `yMod` downstream. The constants `conInt`, `conInt3`, `conInt5` are defined in the model but their values are not documented. |

---

## Section 3: General Comments and Recommendations

| # | Original Text with Issue | Suggested Edit | Rationale |
|---|--------------------------|----------------|-----------|
| 1 | The four implementation paragraphs describe internal blocks (`and2`, `swi`, `latHeaAva`, etc.) in a narrative that closely mirrors the block diagram but is hard to follow without it | Consider adding a schematic figure stored at `modelica://Buildings/Resources/Images/<package-path>/ctlPlaHyb.png` and referenced with `<img alt=\"Control logic schematic for ctlPlaHyb\" src=\"modelica://Buildings/Resources/Images/…/ctlPlaHyb.png\"/>` inside a centred `<p>` | Rule 7 recommends figures for complex blocks. A diagram would make the Implementation section significantly more accessible. |
| 2 | The opening paragraph is a single long sentence containing a parenthetical acronym definition mid-sentence: `integrating heat pump with simultaneous heating-cooling(SHC HP) with multiple modular single-mode heat pumps` — missing space before `(` | Rewrite as: `<p>Block that manages custom calculations for integrating a simultaneous heating-cooling heat pump (SHC HP) with multiple modular single-mode heat pumps to form a hybrid heat pump plant.</p>` | Improves grammatical correctness, adds the missing article `a`, and fixes the missing space before the parenthesis. |
| 3 | `lead/lag` is used inconsistently — sometimes written as `lead-lag` (hyphenated) and sometimes as `lead/lag` (slash) | Standardise to `lead-lag` throughout the `info` text | Consistent hyphenated form is used in the linked class `SortRuntime` and is the Buildings library convention. |
| 4 | The phrase `per the definitions in` before the hyperlink in the first bullet is informal | Replace with `as defined in` | More formal and concise phrasing consistent with technical documentation style. |
| 5 | `Else it shall operate` at the start of the second bullet's second sentence is grammatically awkward | Replace with `Otherwise, the SHC HP shall operate` | Clearer and more formal phrasing; `Else` is a programming keyword and reads awkwardly in prose. |
| 6 | The Implementation section mixes high-level functional description (first bullet list) with low-level block-instance narrative (four paragraphs). The two concerns are at different levels of abstraction | Split into two sub-sections: keep the bullet list under a `<h4>Implementation</h4>` heading and move the block-instance paragraphs to a collapsible note or a dedicated sub-section preceded by `<h5>Block-level details</h5>` | Separating functional intent from implementation detail improves readability for both end-users and maintainers. |
| 7 | No `defaultComponentName` annotation is visible in the provided documentation string, yet the block is a drag-and-drop control block | Verify that `annotation(defaultComponentName="ctlPlaHyb")` is present in the model annotation (it appears in the provided snippet but should be confirmed as correct lowerCamelCase) | Rule 10 mandates `defaultComponentName` for all drag-and-drop classes; the value `"ctlPlaHyb"` is correctly formatted in the existing snippet. |