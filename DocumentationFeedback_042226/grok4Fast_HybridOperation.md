# Documentation Feedback for Buildings.Templates.Plants.Controls.StagingRotation.HybridOperation

## Mandatory Edit Suggestions
These address direct violations of the documentation rules, such as description string format (must be short noun-phrase starting with upper-case, no trailing period) for all parameters, variables, and connectors, and revisions formatting (dates in <i>).

| Original Text with Issue | Suggested Edit | Rationale |
|--------------------------|----------------|----------- |
| Parameter comments like: `Set to true for plants that provide HW` (e.g., for `have_heaWat`) | `Flag for plants providing hot water` | Description strings must be short noun-phrases starting with upper-case, without sentences like "Set to true" (rule 1); current is explanatory. Apply similarly to all Boolean parameters (e.g., `have_chiWat`: change to `Flag for plants providing chilled water`; `have_sorRunTim`: change to `Flag for runtime-based lead-lag rotation`). |
| Parameter comments like: `Vector indicating if each HP is an SHC HP; True=Is SHC HP;False=Not SHC HP` (e.g., for `is_HpShc`) | `Vector indicating SHC heat pumps` | Must be short noun-phrase without semicolons, explanations, or True/False details (rule 1); current is verbose sentence fragment. Apply to others (e.g., `staEquDouMod`: change to `Staging matrix for heating-cooling mode`; `idxEquAlt`: change to `Indices of lead/lag alternate equipment`). |
| Connector comments like: `Heating plant enable` (e.g., for `u1EnaHea`) | `Input for heating plant enable` | Connector descriptions must be noun-phrases; current is too brief and imperative-like (rule 1). Apply to all interfaces (e.g., `uMod`: change to `Input for 2-pipe HP mode`; `yAvaHpShcHea`: change to `Output vector for SHC HP heating availability`; `yStaEqu`: change to `Output for staging matrix`). |
| Internal variable comments like: `Check if both heating plant and cooling plant are enabled` (e.g., for `and2`) | `Logical AND for heating and cooling plant enable` | Internal descriptions must be noun-phrases, not sentences starting with "Check if" (rule 1); current is explanatory verb phrase. Apply to all internals (e.g., `swi`: change to `Switch for staging matrices`; `booScaRep`: change to `Boolean scalar replicator`; `latHeaAva`: change to `Latch for heating availability`; `intEqu`: change to `Integer equality comparator for heating mode`). |
| Info lists: `<ul><li>In heating and cooling mode operation of the plant, the modular single-mode heat pumps shall nelead-lag controlled per the definitions in <a href=\"modelica://Buildings.Templates.Plants.Controls.StagingRotation.SortRuntime\">Buildings.Templates.Plants.Controls.StagingRotation.SortRuntime</a>.</li>...</ul>` (unwrapped text, typo "nelead-lag", >80 chars). | Wrap and fix: `<ul><li><p>In heating and cooling mode, modular single-mode heat pumps follow lead-lag control per <a href=\"modelica://Buildings.Templates.Plants.Controls.StagingRotation.SortRuntime\">SortRuntime</a>.</p></li>...</ul>` | Text must be wrapped in <p> (rule 3); lines exceed ~80 chars soft limit, and hyperlink text uses full path (rule 8: use descriptive like "SortRuntime"). Apply to all <ul><li> in intro and details; fix typo "nelead-lag" to "lead-lag" (rule 10: spell-check). |
| Revisions: `<li>July 29, 2025, by Karthik Devaprasad:<br/>First implementation.</li>` (no <i> for date). | `<li><i>July 29, 2025</i>, by Karthik Devaprasad:<br/>First implementation.</li>` | Rule 9 requires <i> for dates in reverse chronological <ul><li>; current lacks italics, violating format. |
| Hyperlink in details: `<a href=\"modelica://Buildings.Templates.Plants.Controls.StagingRotation.EquipmentEnable\">Buildings.Templates.Plants.Controls.StagingRotation.EquipmentEnable</a>` (full path as text). | `<a href=\"modelica://Buildings.Templates.Plants.Controls.StagingRotation.EquipmentEnable\">EquipmentEnable</a>` | Rule 8 example uses descriptive text, not full path; repeating path is redundant—shorten for clarity. Apply to SortRuntime link similarly. |

## Suggested Edits
These address potential missing information, such as optional info sections (e.g., Assumptions) or references to important parameters, to improve completeness.

| Original Text with Issue | Suggested Edit | Rationale |
|--------------------------|----------------|----------- |
| Info lacks "Assumptions and limitations" section; implies hybrid constraints but not explicit (e.g., "the SHC HP shall operate in Stage 1 for both plants"). | Add after details: `<h4>Assumptions and limitations</h4><p>Assumes SHC HP in stage 1 for simultaneous mode; single-mode HPs follow lead-lag. Requires <code>have_sorRunTim=true</code> for rotation; no fault handling for mode switches.</p>` | Template suggests optional section (rule 4); documenting stage assumptions and param dependencies (e.g., <code>is_HpShc</code>, <code>nHp</code>) clarifies hybrid logic limits. |
| Short intro: `<p>Block that manages custom calculations for integrating heat pump with simultaneous heating-cooling(SHC HP) with multiple modular single-mode heat pumps to create a hybrid heat pump plant.</p>` (long, acronym undefined). | `<p>Block for hybrid heat pump plant control, integrating simultaneous heating-cooling (SHC) HP with single-mode HPs.</p>` | Intro required (rule 4); define SHC on first use for clarity; shorten to <80 chars (rule 3) while retaining purpose. |
| No "Typical use and important parameters" section; params like <code>staEquDouMod</code> mentioned but not guided. | Add before details: `<h4>Typical use and important parameters</h4><p>For hybrid plants, set <code>have_heaWat</code> and <code>have_chiWat</code> true; configure <code>staEquDouMod</code> for simultaneous mode (SHC in stage 1) and <code>staEquSinMod</code> for single-mode. Use <code>have_sorRunTim=true</code> for runtime sorting.</p>` | Template optional section (rule 4); referencing params like <code>nSta</code>, <code>idxEquAlt</code> aids setup for hybrid vs. non-hybrid, tying to <code>is_HpShc</code> vector. |
| Details math/logic: "Uses the heating plant enable <code>u1EnaHea</code> and cooling plant enable <code>u1EnaCoo</code> signals to determine operation mode <code>yMod</code>" (no inline <i> for gates). | Enhance: `<p>Uses <code>u1EnaHea</code> and <code>u1EnaCoo</code> via <i>and</i> gate (<code>and2</code>) to set <code>yHeaCoo</code> and select matrices.</p>` | Rule 5 suggests <i> for inline variables like logical gates; adds precision without length, referencing internal <code>and2</code> (from variables). Apply to latches and switches. |
| Revisions: Single entry with minimal detail. | Expand: `<li><i>July 29, 2025</i>, by Karthik Devaprasad:<br/>First implementation for hybrid mode switching and availability latching.</li>` | Rule 9 encourages descriptive changes; adding scope (e.g., mode signals, latches) improves traceability for initial version. |

## General Comments and Recommendations
- **Clarity and Structure**: Info is technical but jumps between lists—consider a table for functions (rule 6: `<table summary="Hybrid functions"><tr><th>Function</th><th>Implementation</th></tr><tr><td>Mode determination</td><td><code>and2</code> gate</td></tr>...</table>`) under Details. Define terms like "lead-lag" on first use. Hyperlinks good, but add to EquipmentEnable for matrix reqs.
- **Conciseness**: Lines mostly <80 chars, but nested <ul> could flatten; trim repetitions (e.g., "heating-cooling mode" to "simultaneous mode"). Parameter grouping: Suggest `Dialog(group="Hybrid Configuration")` for <code>have_heaWat</code>, <code>is_HpShc</code> (rule 2); `Dialog(group="Staging Matrices")` for <code>staEquDouMod</code>, etc.
- **Compliance Gaps**: No equations (rule 5), but add displayed mode logic if formalized (e.g., `<p align="center" style="font-style:italic;">yHeaCoo = u1EnaHea <i>and</i> u1EnaCoo</p>`). No figures (rule 7): Suggest hybrid schematic `<p align="center"><img alt="Hybrid plant staging modes" src="modelica://Buildings/Resources/Images/Templates/Plants/StagingRotation/HybridModes.png"/></p>` under Typical use. Spell-check: Typo "nelead-lag" fixed in mandatory; ensure "SHC HP" consistent. For complex package, add "See Buildings.UsersGuide for hybrid plants" (rule 10).
- **Improvements**: Emphasize <code>u1HeaCoo</code> input for mode detection. All protected vars described (good). Test algebraic loops (multiple <code>pre</code>). Overall, detailed for experts but needs visuals/sections for broader use; strong on internals but clarify SHC vs. single-mode flows.