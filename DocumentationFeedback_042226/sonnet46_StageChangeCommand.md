# Documentation Review: Stage Change Command Block

---

## Section 1: Mandatory Edits (Rules Being Broken)

| Original Text with Issue | Suggested Edit | Rationale |
|---|---|---|
| Both revision entries are missing `<i>` tags around the dates: `May 31, 2024, by Antoine Gautier:<br/>` and `March 29, 2024, by Antoine Gautier:<br/>` | Change to `<li><i>May 31, 2024</i>, by Antoine Gautier:<br/>` and `<li><i>March 29, 2024</i>, by Antoine Gautier:<br/>` | Rule 9 explicitly requires the date in each revision list entry to be wrapped in `<i>…</i>` tags |
| The OPLR equation is written as a plain italic paragraph: `<p><i>OPLR = Qrequired / Qstage</i></p>` — this is a display equation but does not use the required centred italic format | Replace with `<p align=\"center\" style=\"font-style:italic;\">OPLR = Q<sub>required</sub> / Q<sub>stage</sub></p>` | Rule 5 requires display equations to use a centred italic `<p>` with `align=\"center\"` and `style=\"font-style:italic;\"`; subscripts should also be marked up with `<sub>` |
| Variable names `Qrequired`, `Qstage`, and `OPLR` appear in the introductory paragraph wrapped in `<i>` as `<i>Qrequired</i>`, `<i>Qstage</i>`, `<i>OPLR</i>` — correct use of `<i>` — but the subscript portions "required" and "stage" are not marked up with `<sub>` | Change to `<i>Q<sub>required</sub></i>`, `<i>Q<sub>stage</sub></i>`, and `<i>OPLR</i>` throughout | Rule 5 requires inline variables to use `<i>` with proper sub/superscript markup; plain text subscripts inside `<i>` are not correctly formatted |
| The `<h4>` heading is written with surrounding newlines and extra whitespace: `<h4>\nDetails\n</h4>` | Change to `<h4>Details</h4>` on a single line | Rule 3 requires clean, consistent HTML; extra whitespace inside heading tags is non-standard and may render inconsistently |
| The heading `<h4>Details</h4>` is not a permitted heading label from the Rule 4 template | Replace with `<h4>Implementation</h4>` | Rule 4 defines a fixed set of permitted `<h4>` headings; "Details" is not among them, while "Implementation" correctly describes the content (explanation of "if" vs "when" condition design choice) |
| The description strings for internal parameters `conTraMatStaEquDouMod` and `conTraMatStaEquSinMod` contain the misspelling "tranverse": `"Constant signal for tranverse of staging equation matrix..."` | Change both to `"Constant signal for transpose of staging equation matrix..."` | Rule 10 requires a spell check before committing; "tranverse" is a misspelling of "transpose" |
| Inline condition `<i>OPLR &gt; plrSta</i>` and `<i>OPLR &lt; plrSta</i>` mix a variable name with a parameter name inside a single `<i>` tag | Change to `<i>OPLR</i> > <code>plrSta</code>` and `<i>OPLR</i> < <code>plrSta</code>` respectively | Rule 5 requires variable names in `<i>` and parameter/code references in `<code>`; mixing both inside a single `<i>` tag is incorrect |
| The numeric literal `<i>30</i>&nbsp;s` uses `<i>` around a plain number | Change to `30&nbsp;s` (plain text) | Rule 5 reserves `<i>…</i>` for variable names only; numeric literals must not be italicised |

---

## Section 2: Suggested Edits (Possibly Missing Information)

| Original Text with Issue | Suggested Edit | Rationale |
|---|---|---|
| No `<h4>Typical use and important parameters</h4>` section is present, yet several parameters (`plrSta`, `dtRun`, `dtMea`, `dT`, `dtPri`, `dtSec`, `have_inpPlrSta`) are critical for correct staging behaviour | Add `<h4>Typical use and important parameters</h4>` describing: `<code>plrSta</code>` (staging PLR threshold), `<code>dtRun</code>` (minimum stage runtime and hold duration), `<code>dtMea</code>` (rolling average window), and noting that `<code>have_inpPlrSta</code>` switches between a parameter value and the input signal `<code>uPlrSta</code>` for the staging PLR | Rule 4 recommends this section when important parameters require user guidance; the interaction between `have_inpPlrSta` and `plrSta`/`uPlrSta` is non-obvious and undocumented |
| The parameter `have_inpPlrSta` and its effect on whether `plrSta` or `uPlrSta` is active are not mentioned anywhere in the `info` section | Add a sentence in `<h4>Typical use and important parameters</h4>`: "When `<code>have_inpPlrSta</code>` is true, the staging part load ratio is provided via the input signal `<code>uPlrSta</code>`; otherwise the parameter `<code>plrSta</code>` is used." | The omission leaves users unaware of this configuration option, which changes the source of the key staging threshold |
| The failsafe condition is only referenced via a hyperlink: `see <a href=\"...\">FailsafeCondition</a>` with no inline summary | Add a one-sentence summary before the link, e.g., "The failsafe condition triggers a stage up command when the primary or secondary supply temperature deviates excessively from the setpoint for a duration exceeding the configured threshold." | Users should be able to understand the basic logic without navigating to a separate class; the cross-reference is good practice but should supplement, not replace, an inline description |
| The parameters `dT`, `dtPri`, and `dtSec` (used by the `faiSaf` block) are not described in the `info` section | Add descriptions of `<code>dT</code>`, `<code>dtPri</code>`, and `<code>dtSec</code>` in `<h4>Typical use and important parameters</h4>`, noting they control the failsafe condition thresholds and are passed to the `FailsafeCondition` block | Rule 4 recommends documenting important parameters; these three parameters directly affect the failsafe stage-up logic but are invisible to the reader of the current `info` section |
| No `<h4>Assumptions and limitations</h4>` section is present; the block assumes a non-decreasing stage capacity ordering and that required capacity is always non-negative | Add `<h4>Assumptions and limitations</h4>` stating: (1) stage nominal capacities must be monotonically non-decreasing with stage index; (2) the rolling average window `<code>dtMea</code>` must be long enough to suppress transient spikes; (3) the block does not independently enforce minimum runtime — this must be enforced by the upstream `StageIndex` block | Rule 4 provides this section for non-obvious constraints |

---

## Section 3: General Comments and Recommendations

| Original Text with Issue | Suggested Edit | Rationale |
|---|---|---|
| The `defaultComponentName` is `"chaSta"` which is correct lowerCamelCase | No change needed | Rule 10 compliance confirmed |
| The explanation of "if" vs "when" conditions in the `<h4>Implementation</h4>` section (currently `<h4>Details</h4>`) is well-written and useful; it should be retained after the heading is corrected | No content change needed beyond the heading correction noted in Section 1 | The explanation is technically accurate and addresses a non-obvious design choice that benefits future maintainers |
| The sentence "The required capacity used in logic is a rolling average over a period of `<code>dtMea</code>` of instantaneous values sampled at minimum once every `<i>30</i>&nbsp;s`" mixes measurement implementation details into the main description flow | Consider moving the sampling rate detail ("sampled at minimum once every 30 s") to a `<h4>Implementation</h4>` note, keeping the main body focused on the averaging window `<code>dtMea</code>` | Separating algorithmic behaviour from implementation details improves readability and follows the Rule 4 template intent |
| The phrase "subject to the following requirements" followed by a `<ul>` list and then separate `<ul>` lists for stage-up and stage-down conditions creates a fragmented structure | Consider restructuring with `<h5>` sub-headings "Stage up conditions" and "Stage down conditions" within the main flow, or consolidating the requirements bullet list into an `<h4>Assumptions and limitations</h4>` section | Rule 3 allows `<h5>` sparingly; using it here would improve navigation in a documentation section with multiple distinct condition groups |
| The cross-reference to `Buildings.Templates.Plants.Controls.HeatPumps.AirToWater` is placed in a parenthetical note `(These conditions are implemented in…)` at the end of a paragraph, which may be overlooked | Move the cross-reference to the `<h4>Implementation</h4>` section where implementation choices are discussed, or make it a standalone sentence | Parenthetical cross-references at paragraph end are easy to miss; placing them in the Implementation section gives them appropriate prominence |
| The description string for the internal block `idxLasTru` (`Buildings.Templates.Plants.Controls.Utilities.LastTrueIndex`) has an empty `classComment` in the context table — this suggests the class itself may be missing a description string | Verify that `LastTrueIndex` has a non-empty class-level description string and add one if missing | Rule 1 requires every class to have a description string; an empty `classComment` suggests this may be violated at the sub-component level |